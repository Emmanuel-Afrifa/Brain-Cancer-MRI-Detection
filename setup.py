from setuptools import setup, find_packages

setup(
    name='Brain-Cancer-MRI-Scans-Classification',
    version='1.0.0',
    packages=find_packages(),
    long_description = "This project builds a deep learning model to automatically detect and classify brain tumors from MRI images using convolutional neural networks (CNNs).",
    author = "Emmanuel Afrifa",
    author_email = "emmaquame9@gmail.com"
)
